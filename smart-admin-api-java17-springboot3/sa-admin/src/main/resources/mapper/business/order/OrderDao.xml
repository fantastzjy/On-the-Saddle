<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.admin.module.business.order.dao.OrderDao">

    <!-- 根据订单号查询 -->
    <select id="selectByOrderNo" parameterType="java.lang.String" resultType="net.lab1024.sa.admin.module.business.order.domain.entity.OrderEntity">
        SELECT * FROM m_order 
        WHERE order_no = #{orderNo}
        AND is_delete = 0
    </select>

    <!-- 根据会员ID查询订单列表 -->
    <select id="selectByMemberId" parameterType="java.lang.Long" resultType="net.lab1024.sa.admin.module.business.order.domain.entity.OrderEntity">
        SELECT * FROM m_order 
        WHERE member_id = #{memberId}
        AND is_delete = 0
        ORDER BY create_time DESC
    </select>

    <!-- 根据俱乐部ID查询订单列表 -->
    <select id="selectByClubId" parameterType="java.lang.Long" resultType="net.lab1024.sa.admin.module.business.order.domain.entity.OrderEntity">
        SELECT * FROM m_order 
        WHERE club_id = #{clubId}
        AND is_delete = 0
        ORDER BY create_time DESC
    </select>

    <!-- 根据订单状态查询 -->
    <select id="selectByOrderStatus" parameterType="java.lang.Integer" resultType="net.lab1024.sa.admin.module.business.order.domain.entity.OrderEntity">
        SELECT * FROM m_order 
        WHERE order_status = #{orderStatus}
        AND is_delete = 0
        ORDER BY create_time DESC
    </select>

    <!-- 更新订单状态 -->
    <update id="updateOrderStatus">
        UPDATE m_order 
        SET order_status = #{orderStatus},
            update_time = NOW()
        WHERE order_id = #{orderId}
        AND is_delete = 0
    </update>

    <!-- 统计订单数量 -->
    <select id="countOrdersByStatus" resultType="java.lang.Integer">
        SELECT COUNT(1) FROM m_order 
        WHERE club_id = #{clubId}
        AND order_status = #{orderStatus}
        AND is_delete = 0
        AND is_valid = 1
    </select>

    <!-- 统计活动参与人数（包含待支付状态） -->
    <select id="countActivityParticipants" resultType="java.lang.Integer">
        SELECT COUNT(1) FROM m_order 
        WHERE product_id = #{productId}
        AND order_status IN
        <foreach collection="statusList" item="status" open="(" separator="," close=")">
            #{status}
        </foreach>
        AND is_delete = 0
        AND is_valid = 1
    </select>

</mapper>